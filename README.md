# ÇEVRE - Full Project + AI Swarm

## Quick Start

```bash
# Install
npm install --workspaces
npm run swarm:setup

# Run web
npm run web

# Use AI Swarm
npm run swarm "Add Stories feature"
```

## What's Inside

- ✅ Full Çevre project (Web + Mobile)
- ✅ 15 database migrations (70+ tables)
- ✅ 17 React hooks
- ✅ 8 AI agents for development

## AI Swarm

```bash
# Full feature development (8 agents)
npm run swarm "Feature description"

# Single agent
npm run agent architect "Design feature"
npm run agent backend "Implement feature"
npm run agent security "Audit feature"
```

## Documentation

- `README.md` - This file
- `ai-swarm/README.md` - AI Swarm guide
- `PROJECT_STRUCTURE.md` - File structure
- `packages/supabase/migrations/` - Database schemas

## Setup

1. Install: `npm install --workspaces`
2. Supabase: Create project, run migrations
3. Env: Copy .env.example files, fill API keys
4. Run: `npm run web` or `npm run mobile`

## AI Swarm Setup

```bash
# Install AI Swarm
npm run swarm:setup

# Set API key
export ANTHROPIC_API_KEY=sk-ant-api03-REPLACE_WITH_YOUR_KEY

# Test
npm run agent team-lead "Analyze project"
```

---

**Ready to build!** 🚀
